# Lite order service (saga orchestrator). Waits for Postgres + Kafka before booting so it
# doesn't CrashLoop while infra is still coming up. Image rewritten to ECR by kustomize.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lite-order-service
  namespace: vabags-lite
spec:
  replicas: 1
  selector:
    matchLabels: { app: lite-order-service }
  template:
    metadata:
      labels: { app: lite-order-service }
    spec:
      initContainers:
        - name: wait-infra
          image: busybox:1.36
          command: ["sh", "-c", "until nc -z postgres 5432 && nc -z kafka 9092; do echo 'waiting for postgres+kafka'; sleep 3; done"]
      containers:
        - name: lite-order-service
          image: vabags/lite-order-service
          ports:
            - containerPort: 8081
          envFrom:
            - configMapRef: { name: vabags-lite-endpoints }
            - secretRef: { name: vabags-lite-db }
          startupProbe:
            httpGet: { path: /actuator/health/liveness, port: 8081 }
            failureThreshold: 5
            periodSeconds: 60
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 8081 }
            failureThreshold: 6
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8081 }
            failureThreshold: 6
            periodSeconds: 10
            timeoutSeconds: 5
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits: { memory: "512Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: lite-order-service
  namespace: vabags-lite
spec:
  selector: { app: lite-order-service }
  ports:
    - port: 8081
      targetPort: 8081
