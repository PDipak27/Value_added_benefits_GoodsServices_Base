# Lite billing service (saga participant). Image rewritten to ECR by kustomize.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lite-billing-service
  namespace: vabags-lite
spec:
  replicas: 1
  selector:
    matchLabels: { app: lite-billing-service }
  template:
    metadata:
      labels: { app: lite-billing-service }
    spec:
      initContainers:
        - name: wait-infra
          image: busybox:1.36
          command: ["sh", "-c", "until nc -z postgres 5432 && nc -z kafka 9092; do echo 'waiting for postgres+kafka'; sleep 3; done"]
      containers:
        - name: lite-billing-service
          image: vabags/lite-billing-service
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef: { name: vabags-lite-endpoints }
            - secretRef: { name: vabags-lite-db }
          startupProbe:
            httpGet: { path: /actuator/health/liveness, port: 8083 }
            failureThreshold: 5
            periodSeconds: 60
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 8083 }
            failureThreshold: 6
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8083 }
            failureThreshold: 6
            periodSeconds: 10
            timeoutSeconds: 5
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits: { memory: "512Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: lite-billing-service
  namespace: vabags-lite
spec:
  selector: { app: lite-billing-service }
  ports:
    - port: 8083
      targetPort: 8083
