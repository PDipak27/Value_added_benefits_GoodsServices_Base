# Inventory service — pure saga participant (Postgres + Kafka). No extra env beyond the shared config.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: vabags
spec:
  replicas: 1
  selector:
    matchLabels: { app: inventory-service }
  template:
    metadata:
      labels: { app: inventory-service }
    spec:
      containers:
        - name: inventory-service
          image: vabags/inventory-service:dev
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8082
          envFrom:
            - configMapRef: { name: vabags-endpoints }
            - secretRef: { name: vabags-db }
          startupProbe:
            httpGet: { path: /actuator/health/liveness, port: 8082 }
            failureThreshold: 30
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 8082 }
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8082 }
            periodSeconds: 5
          resources:
            requests: { cpu: "100m", memory: "384Mi" }
            limits: { memory: "640Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: vabags
spec:
  selector: { app: inventory-service }
  ports:
    - port: 8082
      targetPort: 8082
