# Billing service — pure saga participant (Postgres + Kafka).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-service
  namespace: vabags
spec:
  replicas: 1
  selector:
    matchLabels: { app: billing-service }
  template:
    metadata:
      labels: { app: billing-service }
    spec:
      containers:
        - name: billing-service
          image: vabags/billing-service:dev
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef: { name: vabags-endpoints }
            - secretRef: { name: vabags-db }
          startupProbe:
            httpGet: { path: /actuator/health/liveness, port: 8083 }
            failureThreshold: 30
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 8083 }
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8083 }
            periodSeconds: 5
          resources:
            requests: { cpu: "100m", memory: "384Mi" }
            limits: { memory: "640Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: billing-service
  namespace: vabags
spec:
  selector: { app: billing-service }
  ports:
    - port: 8083
      targetPort: 8083
